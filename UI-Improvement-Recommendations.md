# Q-Bank 高端化 UI 改进建议

## 现状评估

当前 Q-Bank 是一个功能扎实的单页面题库管理工具，技术选型务实（Tailwind + KaTeX + Dexie.js + Supabase）。但若想支撑千美元级订阅定价，需要在视觉质感、交互细节、专业工作流三个方面完成从「开发者工具」到「专业生产力软件」的跃迁。

状态标记：
- `[x]` 已在当前 `index.html` / PWA 文件中实现或已有基础能力
- `[~]` 已有雏形，但距离专业付费产品还有明显缺口
- `[ ]` 尚未实现，需要进入后续版本路线图

当前实现快照（2026-05-03）：
- `[x]` 本地 IndexedDB 题库、题目、删除墓碑、版本历史、复习字段
- `[x]` Supabase 配置、邮箱登录、手动上传、手动拉取、云端对比统计
- `[x]` 基于云端 `updated_at` 的上传/拉取冲突保护，已修复多设备漏传和误删风险
- `[x]` PWA 外壳基础文件：`manifest.json` 与 `sw.js`
- `[~]` 智能组卷、标准题手动判分、SM-2 复习联动已有可用雏形
- `[~]` 高级搜索、批量操作、快捷键、标签补全已有基础实现
- `[ ]` 实时自动同步、团队空间、权限、审计、冲突可视化、附件同步、企业合规尚未实现

---

## 一、视觉质感：让每一像素都值钱

### 1.1 字体系统升级

当前问题：用 Google Fonts 加载 Sora + Inter + JetBrains Mono 三套字体，但正文排版层次感不足。

建议：
- 为题干/解析区域引入**专为长文本阅读优化的衬线体**作为第二字体栈，例如 Source Serif 4 或 Charter，用于题目预览区的正文渲染。数学题、语文题在衬线体下阅读体验天差地别。
- JetBrains Mono 用于代码块和公式标注很合适，但应将其字号统一缩小 0.5px，并增加 letter-spacing，避免在中文上下文中显得拥挤。
- 在卡片视图中，题干摘要行高应从当前 1.6 提升到 1.75，并为中英文混排加入 `text-spacing-trim` 或手动微调。

### 1.2 色彩系统——从「黑白灰」到「有温度的专业感」

当前问题：全局用 `#111827`（near-black）和纯白，对比度过高，长时间使用容易视觉疲劳。

建议：
- 将背景从纯白 `#ffffff` 替换为微暖色调，例如 `#fafaf9`（stone-50）或 `#fafbfc`，减少蓝光刺激。
- 为暗色模式增加真正的暗色方案（当前仅有 `class="dark"` 的空壳）。暗色模式不应该是简单的反色，而应该用 `#0d1117`（GitHub dark）作为底色，`#161b22` 作为卡片底色。
- 给六种题型徽章（qtype-standard 等）增加微妙的渐变背景色，而不是纯色块。例如单选题徽章从 `#E6F1FB` 到 `#DBEAFE` 的 135° 渐变，视觉上立刻高级一个档次。
- 掌握状态圆点（q-status-dot）应该加上 `box-shadow: 0 0 6px currentColor` 的发光效果，让状态指示器更有生命力。

### 1.3 间距与留白

当前问题：卡片视图用 `gap-6`（24px），列表用 `gap-3`（12px），但在大屏上显得松散，在小屏上显得拥挤。

建议：
- 引入**流体间距系统**：用 `clamp(16px, 2vw, 32px)` 替代固定的 gap 值，让布局在不同屏幕上自然呼吸。
- 卡片内边距从当前的固定 `p-5` 改为 `clamp(12px, 1.5vw, 24px)`。
- 侧边栏宽度从固定 `w-56`（224px）改为可拖拽调整（200px-320px），让用户根据自己的题库命名习惯自由调节。

### 1.4 微交互与动画

这是高端软件和普通工具最本质的区别。

当前问题：hover 有 transition，但缺少「操作反馈」层面的动画。

具体建议：

**(a) 卡片 hover 效果**
```css
.question-card {
  transition: transform 0.2s cubic-bezier(0.34, 1.56, 0.64, 1),
              box-shadow 0.2s ease;
}
.question-card:hover {
  transform: translateY(-2px) scale(1.01);
  box-shadow: 0 8px 25px rgba(0, 0, 0, 0.06);
}
```
弹性的 scale 变换 + 上浮效果，给人「可以交互」的直觉。

**(b) 按钮点击反馈**
当前 topbar-add-btn 有 `transform: scale(0.97)`，这很好。但所有按钮都应该有这个效果，且应该用 `active:scale-[0.96]` 这样的 Tailwind 类统一处理，而不是零散的 CSS。

**(c) 模态框进入动画**
当前模态框是 `hidden`/`flex` 的二元切换，完全没有过渡。应该改为：
```css
.modal-enter {
  opacity: 0;
  transform: scale(0.95) translateY(8px);
}
.modal-enter-active {
  opacity: 1;
  transform: scale(1) translateY(0);
  transition: all 0.2s cubic-bezier(0.16, 1, 0.3, 1);
}
```

**(d) Toast 通知**
当前有 toast 基础设施，但缺少**进度条自动衰减**动画。一个设置了 3 秒持续时间的 toast 应该在底部有一条细细的倒计时进度条，让用户感知到它会自动消失。

**(e) 列表拖拽排序**
在列表视图下，行应该可以通过拖拽手柄（六个点的 grip icon）调整题目顺序。这是题库管理中极高频的操作——老师经常需要调整出题顺序。

### 1.5 磨砂玻璃与深度

当前 settings-drawer-shell 已经用了 `backdrop-filter: blur(6px)`，这是正确的方向。但建议：

- 将所有模态框的遮罩层统一升级为磨砂玻璃效果（当前部分用的是 `bg-gray-900/50`，部分没有模糊）。
- 在卡片视图区域使用**微妙的噪点纹理**（用 SVG filter 或 CSS `background-image` 实现 0.5% 透明度的噪声），模仿高端纸张的触感。这在 Notion、Linear 等产品中已有成功先例。

---

## 二、专业工作流：让行家觉得顺手

### 2.1 键盘快捷键体系

当前问题：只有零星的快捷键提示（如 `Ctrl+S` 保存、`⌘K` 搜索），没有系统性的快捷键设计。

建议：
- 建立一个完整的快捷键面板（按 `?` 或 `⌘/` 唤出），参考 Notion/Figma 的做法，用半屏抽屉展示所有快捷键。
- 核心快捷键应该包括：
  - `J` / `K` — 在题目列表/卡片中上下导航（Vim 风格，极受专业用户喜爱）
  - `Enter` — 打开当前高亮题目
  - `E` — 快速编辑
  - `Space` — 预览展开/折叠
  - `1-5` — 快速切换题型过滤
  - `R` — 快速切换复习状态（新题 → 复习中 → 已掌握）
  - `⌘+Shift+F` — 全局搜索（当前 ⌘K 可用但不够明显）
- 快捷键提示应该显示在对应按钮的 tooltip 中，并且 tooltip 应该有 200ms 的延迟出现（避免误触干扰）。

### 2.2 批量操作

这是当前最大的功能缺口。处理上千道题的题库时，逐题操作是不可接受的。

建议：
- 在卡片视图下，**长按卡片**（或点击卡片角落的复选框图标）进入多选模式。选中后顶部出现浮动操作栏，支持：批量修改状态、批量添加标签、批量删除、批量导出。
- 列表视图下，在每行最左侧增加复选框列，表头有全选复选框。
- 支持 **Shift+点击** 范围选择。
- 批量操作前应该有预览确认（例如：「即将对 47 道题执行【状态 → 已掌握】」）。

### 2.3 高级搜索与过滤

当前搜索是简单的关键词匹配 + 标签建议，对专业用户远远不够。

建议：
- 支持**搜索语法**：
  - `type:单选` — 按题型过滤
  - `status:复习中` — 按状态过滤
  - `difficulty:hard` — 按难度过滤（需要先加入难度字段）
  - `tag:考点/闭包` — 按标签过滤
  - `before:2024-01-01` / `after:2024-06-01` — 按时间过滤
  - `has:解析` / `-has:解析` — 按有无解析过滤
  - `"完全匹配短语"` — 精确匹配
- 在搜索框聚焦时，下方显示语法帮助提示（可折叠）。
- 保存搜索条件为**智能题库**（类似 Gmail 的 filter → label），这样用户可以为「所有未掌握的高等数学单选题」创建一个永久的动态视图。

### 2.4 题目编辑器增强

当前编辑器是 Markdown 文本区 + 预览窗格，功能足够但体验比较基础。

建议：
- **分屏同步滚动**：编辑区和预览区应该保持大致同步的滚动位置。当用户在编辑区修改第 3 题时，预览区应该自动滚动到对应位置。
- **图片支持**：当前缺少图片上传和粘贴功能。对于数学题、物理题、生物题，图表和示意图是刚需。应该支持：
  - 从剪贴板直接粘贴图片
  - 拖拽图片到编辑区
  - 图片存储在 IndexedDB（Base64 或 Blob），同步时上传到 Supabase Storage
- **模板系统**：用户可以保存题目模板（例如「单选题模板：题干 + 4 个选项 + 解析」），新建题目时一键套用。
- **自动保存指示器**优化：当前 autosave-status 区域太小（字体 11px），应该改为更明显的设计——参考 Apple Notes 的「已保存」指示器风格。

### 2.5 版本历史

这是企业级软件的基本要求。

建议：
- 每次保存题目时，将旧版本存入一个 `question_versions` 表（IndexedDB 内）。
- 在题目编辑界面增加「历史版本」按钮，点击后展示时间线，可以预览和恢复任意历史版本。
- 高亮显示当前版本与选中版本的差异（diff 视图）。

---

## 三、智能功能：AI + 数据驱动

### 3.1 智能标签推荐

当用户输入题干内容后，系统应该自动分析文本并推荐标签。

实现思路：
- 基于规则：检测到「极限」「导数」「积分」等关键词 → 自动推荐 `考点:微积分`
- 基于历史：分析用户已有的标签使用模式，当新题目与已有题目的题干相似度超过阈值时，推荐复用其标签
- 不依赖外部 AI API（保持离线可用），用简单的 TF-IDF + 余弦相似度即可实现

### 3.2 间隔重复（Spaced Repetition）

这是教育类工具最值钱的功能之一。

建议：
- 为每道题增加「下次复习时间」字段
- 基于 SM-2 算法（SuperMemo）或更现代的 FSRS 算法自动计算复习间隔
- 提供一个「今日复习」视图，展示今天需要复习的题目
- 复习时，用户对每道题评分（1-5 分，从「完全不会」到「非常熟练」），系统自动调整下次复习时间
- 在 subbar 中增加「待复习 X 题」的统计项

### 3.3 试卷生成引擎

这是让用户愿意付费的核心功能。

建议：
- **智能组卷**：用户设定条件（题型分布、难度分布、考点覆盖、总分），系统自动从题库中抽取题目生成试卷。
- **A/B 卷**：生成两份难度相当但题目不同的平行试卷（防作弊刚需）。
- **答题卡生成**：自动生成标准答题卡（支持机读卡格式）。
- **试卷排版导出**：将生成的试卷导出为排版精美的 PDF/Word，包含：
  - 学校名称、考试名称、考试时间等表头信息
  - 分大题类型的清晰布局
  - 密封线/装订线（中国考试特有的排版需求）
  - 分值标注

### 3.4 题目质量分析

从数据中提取洞见：
- **难度指数**：基于组卷答题正确率、标准题手动判分和复习评分，自动计算每道题的真实难度
- **区分度**：哪些题目最能区分「已掌握」和「未掌握」的学生
- **重复检测**：扫描题库，标出题干高度相似的题目（可能是重复录入）
- **考点覆盖热力图**：展示各考点的题目数量和掌握率，帮助用户发现薄弱环节

---

## 四、云同步、协作与多端

云同步是这个产品从个人工具走向高端订阅的分水岭。专业用户愿意为它付费，不是因为“能同步”这三个字，而是因为它解决了多设备、多成员、多版本、多年数据沉淀的信任问题。目标不是做一个 Supabase Demo，而是做一个让教师、教研团队、培训机构敢把核心题库资产托管进来的系统。

### 4.1 当前云同步实现状态

已实现：
- `[x]` **Supabase 配置与登录**：支持 Project URL、Anon Key、邮箱密码登录、退出登录
- `[x]` **本地优先架构**：题库和题目存储在 IndexedDB，离线时仍可使用核心题库管理功能
- `[x]` **手动上传本地变更**：本地题库和题目可写入 `qbanks` / `qbank_questions`
- `[x]` **手动拉取云端数据**：云端题库和题目可同步回本地
- `[x]` **云端对比统计**：已有“仅本地 / 仅云端 / 已同步”统计卡片
- `[x]` **删除墓碑**：本地删除已同步题目或题库后，后续上传会把删除传播到云端
- `[x]` **按用户隔离同步时间**：`qbank.lastSync.{userId}` 避免同一浏览器切换账号时互相污染
- `[x]` **服务器时间回写**：上传后使用 Supabase 返回的 `updated_at` 更新本地，减少“刚上传就显示云端较新”的误判
- `[x]` **超时收口**：同步请求卡住时 UI 会恢复可操作，不再永久显示“处理中”
- `[x]` **静默对比不干扰 UI**：登录、初始化、标签页切换触发的后台对比不会污染可见同步状态
- `[x]` **冲突保护雏形**：拉取时如果云端已删除但本地在上次同步后更新过，保留本地记录等待重新上传

仍需注意：
- `[~]` 当前仍是“按更新时间的最后写入优先”策略，适合个人多设备，不足以支撑多人同时编辑
- `[~]` 删除冲突已有保护，但还没有可视化冲突面板，用户无法主动选择保留本地、保留云端或合并
- `[~]` 手动上传/拉取适合早期版本，专业用户需要自动、可解释、可追踪的同步体验

### 4.2 云同步下一阶段：从手动同步到可信同步

P0：把现有同步做成“不会丢数据”的专业底座。
- `[ ]` **同步前快照**：每次上传/拉取前自动写入本地 checkpoint，失败后可一键回滚
- `[ ]` **操作队列 outbox**：本地新增、修改、删除都写入 `sync_outbox`，包含 operation id、entity、payload、createdAt、retryCount
- `[ ]` **幂等同步**：每个操作带唯一 `operation_id`，云端保存已处理操作，重复提交不会重复执行
- `[ ]` **同步日志面板**：展示最近 100 次同步事件，包括上传数量、拉取数量、跳过数量、失败原因
- `[ ]` **局部失败重试**：一批 2000 道题里 3 道失败时，只重试失败项，而不是让用户重新跑整批同步
- `[ ]` **同步健康度**：顶部状态从“已登录”升级为“已同步 / 有本地待上传 / 有云端更新 / 冲突待处理 / 同步失败”

P1：让同步“自动但可控”。
- `[ ]` **自动同步策略**：默认在保存后 3-10 秒 debounce 上传，切换题库/关闭页面前 flush
- `[ ]` **联网恢复自动同步**：监听 `online` 事件，网络恢复后自动处理 outbox
- `[ ]` **跨设备新变更提示**：检测到云端更新时显示非打扰提示：“云端有 12 项更新，可立即拉取”
- `[ ]` **同步节流**：高频编辑时合并同一题目的多次更新，降低 Supabase 写入成本
- `[ ]` **大题库分页同步**：按 `updated_at` + `id` 游标分页拉取，避免一次性 select 全表导致慢查询
- `[ ]` **后台增量对比**：替代现在的全量 compare，降低上万题库的对比成本

P2：让同步“可审计、可恢复、可放心托管”。
- `[ ]` **云端版本历史**：题目每次云端更新写入 `question_versions`，支持任意时间点恢复
- `[ ]` **审计日志**：记录 actor、device、ip、操作类型、对象、diff 摘要，企业版可导出
- `[ ]` **设备管理**：用户能看到已登录设备、最后同步时间、强制登出某设备
- `[ ]` **灾难恢复**：支持“恢复到昨天 18:00 的题库状态”
- `[ ]` **租户级备份**：团队/机构空间每日自动快照，保留 30/90/365 天
- `[ ]` **数据完整性巡检**：定期检查 orphan questions、重复 cloudId、缺失 bank_id、异常时间戳

### 4.3 冲突解决设计

当前冲突策略已经比“盲目覆盖”安全，但专业版必须把冲突变成用户能理解、能处理的对象。

升级思路：
- `[ ]` **冲突收件箱**：所有冲突进入“待处理”列表，而不是静默跳过或覆盖
- `[ ]` **三栏 diff**：左侧本地版本，中间云端版本，右侧合并结果
- `[ ]` **字段级合并**：题干、答案、解析、标签、复习状态、选项分别比较，允许用户逐字段选择
- `[ ]` **安全默认值**：题干/答案冲突默认保留两边；复习数据冲突默认取最新 `smLastReview`
- `[ ]` **批量规则**：允许用户选择“本次全部保留云端标签”“本次全部保留本地解析”
- `[ ]` **冲突教学文案**：用自然语言解释：“这道题在 MacBook 和 iPad 上都被修改过，请选择最终版本”

高端体验关键：冲突面板不是报错页面，而应该像 GitHub Pull Request 一样，让专业用户觉得自己掌控了知识资产的演化。

### 4.4 附件与图片同步

当前题目主要是文本、Markdown、KaTeX；对于高价值专业题库，图片、表格、音频材料、PDF 截图会决定付费上限。

升级思路：
- `[ ]` **附件模型**：新增 `assets` 表，字段包含 `id`、`owner_id`、`bank_id`、`question_id`、`mime_type`、`size`、`hash`、`storage_path`
- `[ ]` **Supabase Storage**：图片和附件上传到私有 bucket，通过 RLS 控制访问
- `[ ]` **内容寻址去重**：用 SHA-256 hash 去重，同一张图在多题复用只存一份
- `[ ]` **离线附件缓存**：IndexedDB 缓存 Blob，离线时仍可预览图片
- `[ ]` **上传队列进度**：附件同步显示单独进度，避免 500MB 图像题库让用户误以为卡死
- `[ ]` **OCR 与公式识别入口**：上传图片后可选“识别题干/识别公式”，这是高价 AI 附加值

### 4.5 团队空间与权限

当前更接近个人账号同步。要让机构愿意支付千美元级订阅，需要团队空间。

建议升级为：
- `[ ]` **Workspace / Organization**：个人空间之外增加团队空间，题库归属于 workspace
- `[ ]` **成员角色**：Owner、Admin、Editor、Reviewer、Viewer
- `[ ]` **题库级权限**：每个题库可设置私有、团队可见、指定成员可编辑
- `[ ]` **题目级审核流**：Editor 提交修改，Reviewer 审核通过后发布到正式题库
- `[ ]` **评论与批注**：团队成员可以在具体题目上留下批注，支持 @ 提醒
- `[ ]` **共享题库目录**：机构内可浏览可用题库，按学科、年级、课程、授权范围筛选
- `[ ]` **只读授权链接**：给外部教师或客户临时查看，不暴露编辑权限

可商业化包装：
- Pro：个人多设备云同步
- Team：团队题库、成员权限、审核流
- Enterprise：审计、SSO、私有部署、年度备份、合规支持

### 4.6 实时协作

通过 Supabase Realtime（WebSocket）实现，但不要一上来追求 Google Docs 式逐字同步。题库编辑更适合“对象级实时协作”。

建议阶段：
- `[ ]` **Presence**：显示谁在线、谁正在浏览当前题库
- `[ ]` **编辑锁**：有人正在编辑某道题时，其他人看到锁定状态，可请求接管
- `[ ]` **实时列表刷新**：其他成员新增/删除题目时，列表出现轻提示并可一键刷新
- `[ ]` **评论实时推送**：题目批注、审核意见实时出现
- `[ ]` **冲突协商**：同一道题被多人修改时，不自动覆盖，进入冲突收件箱

### 4.7 PWA、离线与可靠性

当前状态：
- `[x]` 已有 `manifest.json`
- `[x]` 已有 `sw.js`
- `[x]` IndexedDB 支持离线数据访问
- `[~]` 当前离线体验更多是“能继续用”，不是“让用户明确知道哪些操作待同步”

建议：
- `[ ]` **离线模式提示条**：顶部显示温和提示：“离线中，5 项修改将在联网后同步”
- `[ ]` **待同步计数**：侧边云同步 pill 显示 outbox 数量
- `[ ]` **冲突计数**：有冲突时显示红点，而不是藏在同步面板
- `[ ]` **Service Worker 缓存策略分层**：HTML/JS/CSS 使用 stale-while-revalidate，用户数据不进 SW cache
- `[ ]` **安装引导**：桌面端提示“安装为应用”，iOS 显示添加到主屏幕说明
- `[ ]` **多标签页协调**：同一浏览器多个标签页只允许一个同步 worker 执行，避免重复上传

### 4.8 云同步高端功能补充

这些功能是把产品从“好用工具”抬到“专业人士愿意年度付费”的关键。

- `[ ]` **题库资产保险箱**：年度订阅用户获得 365 天云端快照、误删恢复、人工恢复支持
- `[ ]` **跨机构题库迁移**：支持从旧账号、旧团队、外部系统迁移到新 workspace
- `[ ]` **合规导出包**：一键导出题库、成员、权限、审计日志、附件索引，满足机构归档
- `[ ]` **同步 SLA 面板**：显示过去 30 天同步成功率、平均延迟、失败原因分布
- `[ ]` **私有云/本地部署方案**：Enterprise 可部署到客户自己的 Supabase/Postgres 或私有基础设施
- `[ ]` **数据驻留区域**：让机构选择数据区域（中国大陆之外可选 US/EU/SG）
- `[ ]` **端到端加密题库**：敏感题库可启用客户端加密，服务端只存密文；代价是搜索和协作能力受限，需要作为高级安全模式提供

---

## 五、输出与发布

### 5.1 专业排版导出

当前导出功能（JSON/CSV/Word）比较基础。高端定价需要更专业的输出。

建议增加：
- **LaTeX 导出**：对于数学题，直接导出为 `.tex` 文件，可以编译为专业排版的 PDF
- **试卷模板市场**：提供预设的试卷模板（期中考试、期末考试、单元测验等），用户可以自定义并保存自己的模板
- **导出预览**：导出前先预览效果，所见即所得
- **批量打印**：为所有题目生成打印友好的视图（A4 纸张优化）

### 5.2 数据可移植性

专业人士非常关心数据锁定风险：
- 支持**定时自动备份**到本地文件（用户可配置备份路径和频率）
- 支持导出为 **QTI 标准格式**（Question and Test Interoperability，教育领域标准），确保与 LMS（学习管理系统如 Moodle、Canvas、Blackboard）的互操作性
- 支持导出为 **Anki 牌组**格式（`.apkg` 文件），让用户可以在 Anki 中继续复习

---

## 六、细节清单（快速可落地项）

以下是可以在 1-2 天内完成的改进，投入产出比最高：

1. `[ ]` **骨架屏的 shimmer 动画改为渐变色**——当前的纯白渐变太生硬，改用 `#f1f5f9 → #e2e8f0 → #f1f5f9` 更自然。
2. `[~]` **空状态插图**——已有更友好的空状态文案和 SVG，但还不是完整品牌插画系统；后续应设计一套统一插画语言。
3. `[~]` **loading spinner 统一**——部分 loading 和进度反馈已经优化，但全局仍应统一为同一套 spinner / progress 组件。
4. `[ ]` **卡片的题号显示**——在每张卡片左上角显示一个半透明的题号（如 `#42`），方便用户定位和引用。
5. `[ ]` **搜索框的 ⌘K 徽章在 Windows 上显示为 Ctrl+K**——当前仍需要按系统自动切换。
6. `[x]` **列表视图的行悬停高亮**——列表行已使用 `hover:bg-gray-50`。
7. `[ ]` **返回顶部按钮**——当题库超过 100 道题时，在右下角显示一个半透明的返回顶部浮动按钮。
8. `[x]` **标签输入框的自动补全**——已有基于现有标签的 autocomplete 下拉。
9. `[~]` **移动端底部导航栏优化**——已有底部导航和组卷/复习入口；后续还应补搜索、筛选和同步状态入口。
10. `[x]` **Toast 进度条**——已加入 toast progress bar。
11. `[ ]` **增加 sound design（可选）**——完成重要操作（保存成功、同步完成、删除确认）时播放微妙的音效，参考 macOS 的系统音效设计哲学（极简、不打扰）。
12. `[ ]` **云同步状态小组件**——顶部常驻显示待上传、待拉取、冲突数、最后同步时间，点击进入同步日志。

---

## 七、定价分层建议

基于以上功能，定价不应该只围绕“题目数量”收费，而应该围绕专业用户真正愿意买单的价值收费：可靠同步、数据恢复、团队协作、审计合规、输出质量和节省教研时间。

| 层级 | 价格 | 核心差异 |
|------|------|----------|
| Free | 免费 | 1 个个人题库、每题库 100 题、基础导入导出、无云同步、用于获客 |
| Pro | $19/月 或 $190/年 | 个人无限题库、个人云同步、智能组卷、间隔复习、高级搜索、基础备份 |
| Studio | $49/月 或 $490/年 | 面向专业教师/内容创作者：云端版本历史、附件同步、试卷模板、批量导出、365 天题库保险箱 |
| Team | $39/月/人，5 人起 | 团队空间、共享题库、权限、审核流、评论批注、团队同步日志 |
| Institution | $299-$999/月 | 培训机构/学校教研组：审计日志、统一成员管理、题库资产恢复、QTI/LMS 集成、优先支持 |
| Enterprise | $12k+/年，联系销售 | SSO、私有部署、数据驻留、API、合规导出、专属迁移、定制 SLA |

千美元级订阅不是让个人用户硬付 $1000，而是让专业团队自然达到这个金额：
- 5 人教研组选择 Team：`$39 × 5 × 12 = $2,340/年`
- 个人名师选择 Studio：`$490/年`，再叠加模板市场、AI OCR、题库保险箱等 add-on
- 培训机构选择 Institution：`$299-$999/月`，年度合同天然进入 $3,588-$11,988 区间

可增加的高毛利 add-on：
- **题库保险箱 Plus**：$99/年，365 天快照、误删恢复、人工恢复一次
- **AI OCR/公式识别包**：按量计费，适合扫描纸质试卷导入
- **模板市场抽成**：优秀教师出售试卷模板、题库模板，平台抽成 20%-30%
- **私有迁移服务**：一次性 $500-$3000，帮助机构从 Excel、Word、Moodle、Canvas 迁移题库

---

## 八、总结优先级

如果资源有限，按以下顺序推进：

1. **云同步可靠性 P0** —— 1-2 周，补 outbox、同步日志、快照回滚、同步健康状态；这是所有付费承诺的底座
2. **视觉质感**（字体、间距、微交互）—— 1-2 周，提升第一眼信任感
3. **批量操作 + 高级搜索** —— 2-3 周，解决大题库专业用户的核心痛点
4. **间隔重复 + 组卷判分数据闭环** —— 2-3 周，把练习结果变成复习计划和题目质量数据
5. **智能组卷与专业导出** —— 3-4 周，成为内容生产和考试交付工具
6. **团队空间、权限、审核流** —— 4-6 周，打开千美元级团队订阅
7. **云端版本历史、审计、灾难恢复** —— 4-6 周，支撑 Institution / Enterprise 合同
8. **AI OCR、重复检测、质量分析** —— 持续迭代，作为高毛利 add-on
